# Root directory for filesystem operations and ripgrep searches. Must be an absolute path.
# Example: FILES_ROOT=/home/user/projects
FILES_ROOT=./

# Comma-separated list of absolute paths that the server is allowed to access.
# If empty or not set, defaults to FILES_ROOT only.
# Example: ALLOWED_DIRECTORIES=/home/user/projects/project1,/home/user/data
# For allowing full filesystem access (USE WITH EXTREME CAUTION): ALLOWED_DIRECTORIES=/
ALLOWED_DIRECTORIES=

# Comma-separated list of commands that are blocked from execution.
# Example: BLOCKED_COMMANDS=rm,sudo,mkfs
BLOCKED_COMMANDS=sudo,su,rm,mkfs,fdisk,dd,reboot,shutdown,poweroff,halt

# Default shell to use for execute_command.
# Example: DEFAULT_SHELL=bash (for Linux/macOS) or DEFAULT_SHELL=powershell (for Windows)
# If not set, system default is used.
DEFAULT_SHELL=

# Logging level (trace, debug, info, warn, error)
LOG_LEVEL=info

# MCP Transport mode: "stdio" or "sse"
# If "sse", also set MCP_SSE_HOST and MCP_SSE_PORT.
MCP_TRANSPORT=stdio

# Host for SSE transport (e.g., 0.0.0.0 or 127.0.0.1)
MCP_SSE_HOST=127.0.0.1

# Port for SSE transport (e.g., 3000)
MCP_SSE_PORT=3000

# File Read/Write Limits
# Maximum lines to read at once for read_file (default: 1000)
FILE_READ_LINE_LIMIT=1000
# Maximum lines to write at once for write_file (default: 50)
FILE_WRITE_LINE_LIMIT=50